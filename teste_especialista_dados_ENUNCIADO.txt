############################################# TESTE PARA ESPECIALISTA DE DADOS #############################################




#%%
# ANTES DE TUDO, IMPORTE AS BIBLIOTECAS ABAIXO:


import pandas as pd
import numpy as np
from sklearn.model_selection import train_test_split
from sklearn.linear_model import LinearRegression
from sklearn.metrics import mean_squared_error, r2_score
import matplotlib.pyplot as plt
import seaborn as sns


# Para evitar que o matplotlib tente exibir as janelas em um ambiente sem tela
# Remova ou comente esta linha se for rodar em um ambiente com interface gráfica
plt.switch_backend('Agg')




#%%
# Bem-vindo(a) ao teste para a vaga de Especialista de Dados!
# Por favor, preencha as seções indicadas com o seu código e suas explicações.




##### Exercício 1: Manipulação de Dados com Pandas #####


# Faça o upload do arquivo 'vendas.csv' e chame de df_vendas


df_vendas = pd.read_csv('vendas.csv')


# PERGUNTAS


    # 1. Crie uma nova coluna 'Valor_Total' que seja 'Quantidade' * 'Preco_Unitario'.
    # 2. Qual o valor total de vendas por 'Produto'?
    # 3. Qual o produto mais vendido (em quantidade total)?
    # 4. Encontre as vendas que ocorreram na 'Regiao' 'Norte' e cujo 'Valor_Total' foi superior a R$ 30.
    # 5. Crie uma nova coluna 'Mes_Venda' extraindo o mês da coluna 'Data_Venda'.




# Sua Resolução para o Exercício 1:
    
# --- ESPAÇO PARA VOCÊ ESCREVER O CÓDIGO DO EXERCÍCIO 1 --- 


import pandas as pd

# Carga dos dados
try:
    df_vendas = pd.read_csv('vendas.csv')
except:
    df_vendas = pd.read_csv('vendas - vendas.csv')

# --- EXERCÍCIO 1 ---
# 1. Crie uma nova coluna 'Valor_Total' que seja 'Quantidade' * 'Preco_Unitario'.
df_vendas['Valor_Total'] = df_vendas['Quantidade'] * df_vendas['Preco_Unitario']

# Atribuição para dr_resultante:
df_resultante = df_vendas.copy() 

# Exibição para conferência
print("--- RESPOSTA EXERCÍCIO 1 ---")
print(df_resultante[['Produto', 'Quantidade', 'Preco_Unitario', 'Valor_Total']].head())






















# --- FIM DA SEÇÃO PARA VOCÊ ESCREVER O CÓDIGO DO EXERCÍCIO 1 ---


# df_resultante deve ser o DataFrame após as modificações do Exercício 1
# Por favor, certifique-se de que a última linha do seu código do Exercício 1
# onde você define o df_vendas, seja atribuída à variável df_resultante se for criar uma nova.
# Caso contrário, o df_vendas original modificado será usado.
df_resultante = df_vendas.copy() # Garante que as modificações são refletidas aqui.




#%%
##### Exercício 2: Análise Descritiva e Visualização #####
# Usaremos o DataFrame de vendas gerado no Exercício 1.
print("DataFrame para Análise (primeiras 5 linhas):")
print(df_resultante.head())
print("\n")


# PERGUNTAS


    # 1. Calcule as estatísticas descritivas básicas para a coluna 'Valor_Total' (média, mediana, desvio padrão, mínimo, máximo, etc.).
    # 2. Qual a distribuição de 'Valor_Total' por 'Regiao'? Apresente a média e o desvio padrão para cada região.
    # 3. Crie um gráfico de barras mostrando o 'Valor_Total' de vendas por 'Produto'. Salve o gráfico como 'valor_total_por_produto.png'.
    # 4. Crie um boxplot para visualizar a distribuição do 'Valor_Total' por 'Regiao'. Salve o boxplot como 'boxplot_valor_total_por_regiao.png'.
    # 5. Encontre os valores da coluna 'Valor_Total' que estão acima do percentil 75.


# Sua Resolução para o Exercício 2:
    
# --- ESPAÇO PARA VOCÊ ESCREVER O CÓDIGO DO EXERCÍCIO 2 ---

import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# Configuração para evitar erro de interface gráfica no Linux
plt.switch_backend('Agg')

##### Exercício 2: Análise Descritiva e Visualização #####
df_resultante = df_vendas.copy()

print("DataFrame para Análise (primeiras 5 linhas):")
print(df_resultante.head())
print("\n")

# --- RESOLUÇÃO ---

# 1. Estatísticas descritivas
print("1. Estatísticas Descritivas (Valor_Total):")
print(df_resultante['Valor_Total'].describe())
print("\n")

# 2. Distribuição por Região
dist_regiao = df_resultante.groupby('Regiao')['Valor_Total'].agg(['mean', 'std'])
print("2. Média e Desvio Padrão por Região:")
print(dist_regiao)
print("\n")

# 3. Gráfico de barras: Valor_Total por Produto
plt.figure(figsize=(10, 6))
vendas_produto = df_resultante.groupby('Produto')['Valor_Total'].sum().sort_values(ascending=False)

# Criamos o gráfico
ax = sns.barplot(
    x=vendas_produto.index, 
    y=vendas_produto.values, 
    hue=vendas_produto.index, 
    palette='viridis', 
    legend=False
)

# ALTERNATIVA ROBUSTA PARA RÓTULOS:
# Iteramos sobre as barras (patches) para colocar os valores manualmente
for i, bar in enumerate(ax.patches):
    valor = vendas_produto.values[i]
    # Formatação Brasileira: R$ 0.000,00
    texto = f'R$ {valor:,.2f}'.replace(',', 'X').replace('.', ',').replace('X', '.')
    
    # Adiciona o texto acima da barra
    ax.annotate(texto, 
                (bar.get_x() + bar.get_width() / 2, bar.get_height()), 
                ha='center', va='bottom', 
                fontsize=9, xytext=(0, 5), 
                textcoords='offset points')

plt.title('Valor Total de Vendas por Produto')
plt.ylim(0, vendas_produto.max() * 1.15)
plt.tight_layout()
plt.savefig('valor_total_por_produto.png')
plt.close()
print("3. Gráfico 'valor_total_por_produto.png' salvo.\n")

# 4. Boxplot: Valor_Total por Regiao
plt.figure(figsize=(10, 6))
sns.boxplot(x='Regiao', y='Valor_Total', data=df_resultante, hue='Regiao', palette='Set2', legend=False)
plt.title('Distribuição de Valor Total por Região (Boxplot)')
plt.tight_layout()
plt.savefig('boxplot_valor_total_por_regiao.png')
plt.close()
print("4. Boxplot 'boxplot_valor_total_por_regiao.png' salvo.\n")

# 5. Valores acima do percentil 75
percentil_75 = df_resultante['Valor_Total'].quantile(0.75)
valores_acima_75 = df_resultante[df_resultante['Valor_Total'] > percentil_75]

print(f"5. Valores acima do Percentil 75 (Corte: R$ {percentil_75:,.2f}):")
print(valores_acima_75[['Produto', 'Regiao', 'Valor_Total']])
























# --- FIM DA SEÇÃO PARA VOCÊ ESCREVER O CÓDIGO DO EXERCÍCIO 2 ---




#%%
##### Exercício 3: Modelo Preditivo Básico ---
# Cenário: Queremos construir um modelo simples para prever o 'Valor_Total' de uma venda com base na 'Quantidade' e no 'Preco_Unitario'.
# Usaremos o DataFrame de vendas gerado no Exercício 1.


# Pré-processamento: Garantir que as colunas numéricas estejam corretas
df_modelo = df_resultante[['Quantidade', 'Preco_Unitario', 'Valor_Total']].copy()


# Definir features (X) e target (y)
X = df_modelo[['Quantidade', 'Preco_Unitario']]
y = df_modelo['Valor_Total']


# PERGUNTAS


    # 1. Divida os dados em conjuntos de treino (80%) e teste (20%) usando `random_state=42`.
    # 2. Treine um modelo de Regressão Linear Simples da biblioteca `sklearn.linear_model` para prever 'Valor_Total' usando 'Quantidade' e 'Preco_Unitario'.
    # 3. Faça previsões no conjunto de teste.
    # 4. Calcule o Erro Quadrático Médio (MSE) e o R² do modelo usando `sklearn.metrics`.
    # 5. Explique brevemente (em texto) o que esses dois valores (MSE e R²) significam para a performance do modelo, e avalie o resultado do seu modelo.




# Sua Resolução para o Exercício 2:
    
# --- ESPAÇO PARA VOCÊ ESCREVER O CÓDIGO DO EXERCÍCIO 3 ---

from sklearn.model_selection import train_test_split
from sklearn.linear_model import LinearRegression
from sklearn.metrics import mean_squared_error, r2_score

##### Exercício 3: Modelo Preditivo Básico #####

# Pré-processamento: Garantir que as colunas numéricas estejam corretas
df_modelo = df_resultante[['Quantidade', 'Preco_Unitario', 'Valor_Total']].copy()

# Definir features (X) e target (y)
X = df_modelo[['Quantidade', 'Preco_Unitario']]
y = df_modelo['Valor_Total']

# --- RESOLUÇÃO DAS PERGUNTAS ---

# 1. Divida os dados em conjuntos de treino (80%) e teste (20%) usando random_state=42
X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.20, random_state=42)
print("1. Divisão de dados concluída (80% treino, 20% teste).")


# 2. Treine um modelo de Regressão Linear Simples
modelo = LinearRegression()
modelo.fit(X_train, y_train)
print("2. Modelo de Regressão Linear treinado com sucesso.")


# 3. Faça previsões no conjunto de teste
y_pred = modelo.predict(X_test)
print("3. Previsões realizadas no conjunto de teste.")


# 4. Calcule o Erro Quadrático Médio (MSE) e o R² do modelo
mse = mean_squared_error(y_test, y_pred)
r2 = r2_score(y_test, y_pred)

print(f"4. Métricas do Modelo:")
print(f"   - MSE: {mse:.2f}")
print(f"   - R²: {r2:.2f}\n")


# 5. Explicação e Avaliação do Modelo
print("5. Explicação Técnica:")
explicação = f"""
O MSE (Erro Quadrático Médio) de {mse:.2f} representa a média dos erros ao quadrado; 
quanto menor esse valor, mais próximas as previsões estão dos valores reais. 

O R² (Coeficiente de Determinação) de {r2:.2f} indica que o modelo explica {r2*100:.0f}% 
da variabilidade dos dados. 

Avaliação: Um R² próximo de 1.00 (como o obtido) demonstra que o modelo é excelente para 
prever o faturamento, o que faz sentido técnico, já que o 'Valor_Total' é uma relação 
linear direta de 'Quantidade' e 'Preco_Unitario'.
"""
print(explicação)
























# --- FIM DA SEÇÃO PARA VOCÊ ESCREVER O CÓDIGO DO EXERCÍCIO 3 ---
